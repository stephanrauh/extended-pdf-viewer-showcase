# FTP credentials for deploying the showcase to pdfviewer.net.
# Copy this file to `.env` (same folder) and fill in the real values.
# `ftp/upload.ts` reads these via dotenv; `.env` is gitignored, this template is not.

ftphost=ftp.example.com
ftpuser=your-ftp-user
ftppassword=your-ftp-password
